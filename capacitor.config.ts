import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'au.BountyByBree.ShopByBree',
  appName: 'ShopByBree',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
